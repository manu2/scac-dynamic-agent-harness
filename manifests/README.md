# Pilot-manifest templates

Templates in this directory are **not** frozen study manifests and do not
authorize any provider request. A pilot owner must copy, fill, review, commit,
and hash a new immutable manifest before it can be bound in `PROVENANCE.json`.
